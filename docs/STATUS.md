# STATUS — platform-agent

최종 갱신: 2026-09-01

> 현재 구현 상태 / 검증 baseline / active focus / open risks. **≤120줄** 유지.

---

## 검증 Baseline (실제로 돌린 것만)

- `make check` → **2381** (2026-09-01 로컬 macOS·py3.13 · **CI도 같은 게이트**, ubuntu·py3.13 — **CI 숫자는 PR 체크가 권위**). **+13**: **테이블은 생겼는데 아무것도 안 들어 있었다**(M46) — `MEASURABLE`이 **행 수**를 안 봐서 *"잴 수 있다"*고 말해 놓고 물으면 0행이었다(빈 절이 0으로 읽히는 그 실패가 **한 층 위에서**). ⚠️**같은 분기가 인쇄하던 `bq` 명령은 돌지 않았다** — 도달하려면 라이브 내보내기가 있어야 해서 **한 번도 실행된 적이 없었다**(문서 §4엔 맞게 적혀 있었다 — **복사본 둘 중 하나가 뒤처진다**). ⚠️**인쇄된 지시도 주장이다.**
- `make check` → **2368** (2026-09-01, 같은 조건). **+10**: **시그니처는 필수라 했고 본문은 선택이라 했다**(M45) — `record_incident`의 다섯 필드가 본문에선 `x or "기본값"`인데 선언은 필수 `str`이었다. ⚠️**진짜 발견은 그 기본값이 정책인데 아무도 안 묻고 있었다는 것**: **MANUAL**=실행하지 **않는** 모드 · **P3**=척도의 **바닥**. 둘 다 **축의 안전한 끝**이다.
- `make check` → **2358** (2026-09-01, 같은 조건). **+8**: **계약이 모두가 부르는 메서드를 빠뜨렸다**(M44) — `ExecutionAdapter`가 `parameters_for_action`을 선언 안 하는데 넷이 다 구현하고 executor가 **베이스 타입으로 부르며 테스트는 그 이름을 0번 언급**했다. **찾은 건 mypy다.** ⚠️**provider를 인자로 받은 함수가 그걸 무시하고 AWS를 하드코딩해도 전체 스위트가 초록이었다**(M23과 같은 모양) — 내 면제가 만든 탈출구라 닫았다.
- `make check` → **2350** (2026-09-01, 같은 조건). **+4**: **게이트가 안 돈 PR을 "CI 초록"으로 읽었다**(M43, Risk 12⑨). ⛔**`main`은 무방비였던 적 없다**(보호가 `check`를 요구) — **스택 PR**에서만 job이 안 돌았고 그 목록엔 **`check` 행이 아예 없어** 통과와 구분이 안 됐다.
- `make check` → **2346** (2026-09-01, 같은 조건). **+3**: **셋째 형제는 mypy였다**(M42) — `[tool.mypy]`가 vendored 경로를 안 적어 `mypy src/`가 **수집 단계에 죽고 있었다**. ⚠️**형제를 세는 가드 자신이 둘만 셌다**(파일명이 `both_tools`) · ⚠️**가드의 가드도 변이가 시켜서 만들었다**(`TOOLS`가 손으로 적은 dict라 하나 빼도 초록) · ⚠️**mypy 252는 증분 캐시였다**(Risk 12⑦ `.pyc` 계열).
- `make check` → **2342** (2026-09-01, 같은 조건). **+3**: `onprem` extra에서 `mlx-lm`을 뺐다(M41). ⚠️**기록된 근거는 적을 당시 참**이었고 **stale해진 게 아니라 더 나빠졌다** — 하한은 리눅스 resolve가 진짜 실패하는데 리졸버가 고르는 현재 버전은 **설치되고 엔진만 조용히 빠진다**. ⚠️**내가 만든 가드 하나가 주석으로 통과했고**, 그걸 "가드 없음"으로 읽은 것도 **한 파일에만 물어서**였다(Risk 12⑦ 재발).
- `make check` → **2339** (2026-09-01, 같은 조건). **+7**: 4a를 접으며 `test_amp_cost_handles`를 지우지 않고 계약을 함수로 빼 **합성 표**에 물렸다(M40). ⚠️**첫 판은 규칙 하나가 다른 규칙의 그림자로 세어져 있었다** — 와일드카드 검사를 지워도 초록(M17 재발) ⇒ 위반에 **코드**를 붙이고 케이스가 **어느 규칙이 물어야 하는지** 지정한다. 변이 7 red.
- `make check` → **2332**(08-30, **M39가 권위**) — ⚠️**2337에서 내려갔고 그게 옳다**: Azure 배선이 parity의 **정당화 7건을 한꺼번에 무너뜨려 −14**, 새 가드 +9. **결함이 닫히면 그 결함을 설명하던 줄도 사라진다 ⇒ 단조증가 지표가 아니고 줄어든 숫자는 분해해 적을 것.** 2291→2337의 +37과 **일곱 개의 조용한 실패**는 **M19~M26이 권위**, 내역은 archive. ⚠️**렌즈는 여섯째 층에서 말랐다** — 다섯 층에서 다섯 결함, 여섯째는 **범위**.
- `make check` → **1862~1856**(08-13, **M18이 권위**) · **1825**(08-12) · **1789**(08-11, M17) · **1737~1699**(08-09 → **M16**). 상세는 archive가 권위. **여기 남길 함정 셋(08-09)**: 라이브 `gcloud` 호출(21.97s→0.02s — ⚠️**같은 계열이 08-15에 재발했고 D49가 일반화해 집행**) · 반증 중 **`.pyc`에 속았다** · `spend-watch`는 **터미널을 안 열면 검사도 없다**.
- (이전 baseline — gate **1697 이하** / 07-10~08-09 및 `main` 보호 확인 →
  `docs/archive/{status-baseline-2026-07,progress-2026-08}.md` · `COMPLETED_SUMMARY` M13·M14.)

## Active Focus

> **"무엇이 도는가"는 여기 적지 말 것** — `AGENT_BRIEF` **Snapshot**과 `COMPLETED_SUMMARY`
> **M0~M45**이 권위다. 이 파일은 **무엇을 믿어도 되는가**만 답한다.

**Phase 4 — 4a는 닫혔고(08-30) 접혔다(09-01)**: DoD 넷 + 실제 청구 **$0.00**(프리티어 `Always Free` 40M) → 워크스페이스·IAM 사용자·키 **셋 다 삭제**. ⚠️**믿으면 안 되는 요약 셋**: **"어차피 공짜"**(절벽이 요율→**한도**로 옮겼을 뿐, 필터 없음 **128배**) · **"$1.42"**(720h 연속 가동 **가정**) · **"$0.00이라 접었다"**(대가는 **장기 키**였다). ⚠️**D48은 안 죽었다**. 권위 = 계획서 **§10·§11** · **M38·M40** · **D50 Folded**. **다시 열지 말 것.**

**과대 해석 금지(현재 유효한 것만)**
- 스코프·배포 신원 · 이미지 서명 배포 게이트는 전부 **옵트인**(→ Risk 3·6).
- 자격증명이 테넌트-바운드인 건 **온프렘뿐**(→ Risk 10) · 파티션된 읽기 경로는 **둘뿐**.
- `main` 보호는 **게이트 집행**이지 리뷰 집행이 아니다(코드 소유자 리뷰 off → D43).
- **어드미션 미도입** · **CODEOWNERS는 라우팅** · Phase 5는 **경계까지**(UI·PR 생성 없음).

**반복 확인**: 기록된 이유가 진짜 구속 조건이 아닐 때가 많다(M13→M15). 단 **늘 그런 건 아니고** 성립한 쪽이 더 많았는데
**그래도 값은 나왔다** — 08-13에 "안 봤다"를 시험하니 **볼 게 없다는 게 측정으로 바뀌었고** 그 과정에서 **절반만 묻던 가드**가 나왔다.
**"없다"는 어떻게 봤는지까지**(⚠️08-30에 한 리전만 보고 "없다"로 답할 뻔했다 → Risk 4) · **목록을 훑을 땐 목록이 무엇의
그림자인지부터**(Risk 12④ⓐ) · **직전 세션의 Next를 그대로 따라갈 것**(08-13: "덮는 테스트가 있나" → 전부 **모양만 물었다**).
⚠️**열린 항목 자체가 틀릴 수 있다(08-15)**: ⓑ는 "매핑이 없다"였는데 **네 provider 전부 있었고
기록보다 한 달 먼저** 있었다 — **stale이 아니라 쓰일 때부터 틀린 기록**이다. `git log -L`로
**언제부터 있었는지까지** 물어야 stale과 오기를 가른다. 그리고 ⚠️**"선언됐는데 안 읽힌다"는
자동으로 결함이 아니다** — 판단 기준은 **읽는 쪽의 provider 간 비대칭**이지 선언의 고아
상태가 아니다(→ `NEXT_PLAN` 유지 규약의 `provider` 항).

## Open Risks / Gaps

1. **CDK 배포 시 Vercel context 필수** — ⚠️context 미지정 배포가 **실제로 07-11 OIDC provider를 삭제**해 대시보드가 조용히 DEMO FALLBACK으로 강등됐다(07-18 복구). diff/deploy는 반드시 `-c vercelTeamSlug=men16922s-projects -c vercelProjectName=platform-agent`. 로컬 pip 번들링(arm64↔amd64) 주의.
2. **GCP/Azure 인시던트 스토어는 보관 정책이 없다 — 단 "실 데이터 삭제"는 틀렸다** — 코드 갭은 유효(둘 다 TTL 미설정)지만 **스토어가 아예 없어 지울 데이터 0**(런북 스토어도 마찬가지 · **08-30 재확인 여전히 0** — GCP엔 `coffee-menu`, Azure엔 `roadpilot`뿐이고 **둘 다 남의 프로젝트**) → **Phase 4** 선행. 증거 `gcp-azure-retention-nothing-to-delete.log`.
3. **⚠️ 스코프·배포 신원은 집행 가능하지만 옵트인이다(D38·D39)** — **기본값은 미설정**이라
   자격증명 env 없이는 인시던트는 전부 거부, 배포는 ambient(=cluster-admin) —
   **"설정하면 집행되고, 안 하면 조용히 안 된다."** 묻기 `probe_scope_reachability.py`·`make
   deploy-identity-check` · 켜기 `make scope-credentials`·`make deploy-identity`.
   **남은 것**: 배포 신원의 테넌트 구분(결정 5 C/D) · 클라우드 3종은 Risk 10.
4. **세 클라우드 다 기본값이 안심시키는 답을 준다** — 경위 **M16**, 측정 로그가 권위
   (`what-is-actually-billing-2026-08-{12,30}.log` · **`the-measurement-is-the-bill.log`(09-01)**).
   ⚠️넷째는 클라우드가 아니라 **우리 추정치의 가정**이었다(4a "수백 시계열" → 실측 52,438).
   **읽는 법**: 셋 다 `make spend-check`에 박혔고 · ₩20 예산 → **₩28,000** · ⚠️**CE 지연 이틀+ —
   최근 줄의 0은 지연이지 0이 아니다** · ⚠️**MTD로 "지금"을 답하지 말 것**(→`--granularity DAILY`).
   ⚠️**"전 리전 EC2 0대"가 한 번 틀렸다**(정지 t3.medium, 08-16 종료).
   **지금(09-01 실측)**: AWS 바닥 **$0.0034/일**(도는 EC2 0대). ⚠️**서비스별로 가르니 결론이 바뀐다** — 08-20~09-01 총 **$0.2380** 중 **Cost Explorer가 $0.2000 = 84%**이고 진짜 인프라는 **월 ≈$0.09**다. **관측이 관측 대상을 만든다 — 조회 남발 금지는 산수다.** · Azure **쟀다 — 기록된 결론이 뒤집혔다**: 서버가 **`clienttype-retry-after`로 대기 시간을 말한다**(5→**40**; 고갈된 버킷은 `clienttype` 하나). 40을 지키니 **200** — ACR **₩76.67**·Cosmos DB ₩0(전액 남의 것). ⚠️*"재시도가 답이 아니었다"*는 **간격이 요구값 아래**였던 것(20초<40초) — **고정 간격 재시도는 영구 실패와 구분되지 않는다**(`az rest`는 헤더를 버린다). · GCP **아직 답이 없다**(01:53Z 생성·14:08Z에도 **0행** — ⚠️**'₩0'이 아니다**, 판정은 **09-02 01:11Z 이후**). ⚠️**세 답의 모양이 다르다 — 하나로 요약하지 말 것.**
   ⚠️**세는 함정 다섯**: 리전 한정 조회의 `[]` · `AccessDenied` · 429 · **`bq show` 실패** — **전부 "없다"가 아니다**, 그리고 ⚠️**빈 테이블도 측정이 아니다** ·
   ⚠️**로그 창이 사건보다 넓으면 지나간 실패를 현재로 읽는다**(09-01, remote_write).
   ⛔**4a 접기(D50)는 2026-09-01에 완료**(셋 다 삭제, 8리전 스윕 0 — M40) ⇒ **AMP는 지출·관측에서 빠진다.**
   ⚠️**09-01 후속**: kind를 띄우니 **삭제된 워크스페이스로 계속 403**이었다 — **값 파일과 배포된 릴리스는 다르다**(helm rev 5→6). ⚠️**`--reuse-values`는 지우려는 그 블록을 되살린다.**

5. **k3s는 집행하지만 proven 집합엔 없다 — D40으로 닫힘(08-01)** — 집행은 라이브 증명(07-29),
   시맨틱은 미증명. 반복된 "피어 테넌트 부재"는 **참이지만 구속력이 없었다**(진짜 이유는 **순환**
   — 승격하려면 먼저 승격해야). 여는 조건: k3s-lab에 워크로드 → D40 · `docs/plans/2026-08-01-k3s-proven-substrate.md`.
6. **공급망: 생산자·소비자·CI 키리스는 섰고, 어드미션은 업스트림 대기** — **M15가 권위**. `make sign-image` + `image_trust`(배포 직전 거부, **exit 2도 거부**) + CI 키리스까지 라이브.
   **과대 해석 금지**: 배포 게이트는 **옵트인**(`PLATFORM_REQUIRE_SIGNED_IMAGES`)이고 **온프렘 진입점 하나**만 덮는다 · 로컬 `make sign-image`는 **dev 키** · Rekor는 **영구 공개** ·
   **어드미션 미도입**(cosign v3 ↔ policy-controller 저장 위치 불일치, v2는 통과 — 양방향 실증). ⛔**재개 조건 미충족, 08-30 확인**: `sigstore/policy-controller#1406`(번들 스펙)이 **여전히 open**이고 cosign **v3.1.2의 `sign`·`attest` 어디에도 `--new-bundle-format`이 없다**(우리 경로는 `sign --key`).
   ⚠️**우리 게이트는 같은 이미지에 VERIFIED를 준다** = **"검증됨"이 도구마다 다르고 우리는
   못 본다**. 증거 `docs/evidence/cosign-admission-kind-attempt.log` · M15.
7. **TS 타입은 네트워크 데이터를 보증하지 않는다** — 라이브 페이지가 `posture.namespaces.length`로 죽는 동안 `tsc`는 내내 초록이었다(구버전 페이로드). 롤링 중엔 허브가 두 버전을 동시에 서빙 → **푸시 신규 필드는 항상 optional + 폴백**.
8. **PSS restricted 아래에서 애드온 차트는 기본값으로 안 돈다** — 파드는 admission에서 거부되는데 **Argo는 Synced로 보인다**(파드 0개인 채). **새 애드온마다** 렌더된 파드 스펙을 테넌트 ns에 `kubectl apply --dry-run=server`로 던져 API 서버에 직접 물을 것. values 파일은 에러가 아니라 **안 읽히는 방식으로** 실패한다(키 철자가 차트마다 다름).
9. *(해소)* Capsule deprecation(08-02, D41) · GCP 예산 상시 발화 → ₩28,000(08-09) · 리포트 스트림 분리(08-11, M17).
10. **GCP/Azure 자격증명은 아직 테넌트-바운드가 아니다** — 스코프는 **어느 네임스페이스를
   건드릴지**만 정하고 토큰을 테넌트에 안 묶는다(GCP는 프로젝트 전역 신원 하나, Azure는 ARM에서 클러스터 admin kubeconfig). 자격증명 자체가 경계인 건 **온프렘뿐** → Phase 4. ⚠️**08-30부터 Azure 경로가 실제로 열려 있다**(M39 배선) — 오늘 대상 자원이 0개라 blast radius가 0인 것이지 **경계가 생긴 게 아니다**.
11. **Dashboard dependency audit — 해소됨(08-30). `npm audit` 0건.** ⚠️**"upstream 대기"로 다시 닫지 말 것** — 그 기록이 **세 군데 다 틀렸다**: 적힌 *"PostCSS moderate 2건 · 빌드타임"* 대신 실측은 **critical 2 · high 6**이고 **런타임**이었다(`next-auth`가 *"auth checks **fail open**"*인데 **승인 UI 포함 런타임 8파일**에). ⚠️반증 실험이 **`--force` 하나뿐**이었고 그 답을 항목 전체의 성질로 썼다. 조치·검증 상세는 **증거 로그가 권위**(`the-dashboard-audit-record-described-a-different-risk.log`).
12. **게이트의 초록에는 조건이 붙는다 — 아홉이 같은 계열(2026-08-08~09-01)** —
   ⑨**워크플로가 안 도는 것도 초록으로 보인다(09-01, M43)**: `gate.yml`이 `pull_request: branches: [main]`이라 **스택 PR엔 job이 안 돌았고**, `gh pr checks`엔 **`check` 행이 아예 없어** 통과와 구분이 안 됐다 — 실제로 PR #58을 그렇게 읽었다. ⛔**`main`은 무방비였던 적 없다**(보호가 `check`를 요구). **②의 한 층 위**: 거기선 건너뛴 **테스트**였고 여기선 안 돈 **워크플로**다. 필터를 지워 **모든 PR이 게이트를 받는다**.
   ①**시간**: 픽스처가 절대 시각을 하드코딩하고 생산자가 살아 있는 시계로 창을 걸면 통과는 **달력이 움직이기 전까지만** 참이다(07-29 픽스처 → 08-05 만료). ②**환경**: 게이트가 **선언되지 않은 패키지** 위에서 통과하고 있었다 — CI가 잡았고 로컬에선 원리상 안 드러난다(`requires-python = ">=3.11"`은 **08-30에 재서 거짓**: fresh 3.11은 **2 failed**, 같은 의존성 fresh 3.13은 초록 ⇒ 선언 셋을 3.13으로 맞췄다. ⚠️같은 스윕에서 **`[tool.mypy] strict = true`를 아무도 안 돈다**도 나왔다 — 253 errors/88 files라 **주석+가드**로 남겼다). 역방향도 실측: **skip은 실패가 아니라서 검사 안 하는 게이트와 통과한 게이트가 같은 색**이다 → **게이트 숫자엔 날짜와 잰 기계를 붙일 것.** ⚠️**08-30에 여덟째**: 문서화된 **`make dev-up`이 새 클론에서 못 돌았다**(`.venv-mlx`를 만드는 타깃이 없었고 둘은 `nohup`이라 **조용히** 실패 — `mlx-setup` 추가 + 세 지점 사전검사, +5).
   ③**하중**: **행복 경로만 태운 가드는 하중을 받지 않는다**(안전망을 지워도 14개가 초록) → **새 가드는 지워 보고 red를 확인할 것.** ⚠️**둘째 얼굴**(08-12) — 분기를 **깨져야만 열리는 플래그**로 걸면 *원리상 도달 불가*다 ⇒ **조건 축은 값을 넣어 명시적으로 걸 것.** ④**관측 지점 — M17이 권위**: **가드는 독자가 읽는 그 물건에 대고 물을 것.** ⓐ**결함을 그 그림자로 세지 말 것** · ⓑ**"stderr 금지"가 아니다** · ⓒ**절반만 묻는 가드** · ⓓ**문이 넷** · ⓔ**"클러스터가 필요하다"는 잰 게 아니었다**(합 6건). ⚠️**ⓐ가 09-01에 두 번 재발**(M40·M41): 합성 표를 `violations != []`로만 물어 **와일드카드 검사를 지워도 초록**이었고, `"mlx-lm" in Makefile`은 **주석으로 통과**했다 ⇒ **위반에 코드를 붙이고 케이스가 어느 규칙이 물어야 하는지 지정할 것.** 남은 건 `slack_live_approval`.
   ⑤**가드 자신의 값과 하네스(M18)**: **기본값과 같은 값을 고른 픽스처는 가드가 아니다** · **초록으로 안 돌아오는 복구는 복구가 아니다** · **한 블록 두 파일**이면 **provider 간 일치를 묻는 가드**를 같이 둘 것. ⚠️**⑥형제 집합(08-13에 셋, 08-15에 둘, 09-01에 하나 — M18~M20·M42)**: 가드가 **형제 중 하나만 순회**하면 나머지는 안 물어진 채다. 셋째는 **그걸 찾으려고 만든 스윕 안에서**, 09-01의 것은 **형제를 세는 가드 자신**(파일명이 `both_tools`)이었다 → **세는 순간 전부 세고, 산문이 참이어도 임포트 줄이 범위다.** ⑦**변이 하네스 자신(08-15에 다섯, 08-30·09-01에 더)**: **복구 확인이 `.pyc`에 속았다** ⇒ **앞에 캐시를 지울 것** · **틀린 파일에 물었다**(09-01에 두 번 더) · **변이를 적용하지 않고 변이를 쟀다**(09-01) → **변이 적용부터 확인하고, 기준선을 먼저 찍을 것** · **변이·실행·복구는 한 스크립트에**. 증거는 **M17~M20이 각각 자기 로그를 가리킨다**.
- (그 밖의 해소 이력은 `PROGRESS_LOG`/`docs/archive/` 참조.)
