# STATUS — platform-agent

최종 갱신: 2026-08-30

> 현재 구현 상태 / 검증 baseline / active focus / open risks. **≤120줄** 유지.

---

## 검증 Baseline (실제로 돌린 것만)

- `make check` → **2332** (2026-08-30 로컬 macOS·py3.13 · **CI도 같은 게이트**, ubuntu·py3.13 —
  **CI 숫자는 PR 체크가 권위**). ⚠️**같은 날 2337에서 내려갔다**(M39): Azure executor 배선이
  parity의 **정당화 7건을 한꺼번에 무너뜨려 −14**, 새 가드 +9 ⇒ **−5**. **결함이 닫히면 그 결함을
  설명하던 줄도 사라진다 ⇒ 게이트 숫자는 단조증가 지표가 아니고 줄어든 숫자는 분해해 적을 것**
  (Risk 12② 계열). 2291→2337의 +37 내역은 archive. **M19~M26이 권위** — 일곱 다 **조용히** 실패했다:
  ①`resource_types` 미판독(어긋난 쌍 **67/81**) ②운영자 `severity_hint`가 두 모델에 안 닿음
  ③`reason`이 규칙 이름을 잃어 **P3↔P1** 뒤집힘(형제가 provider가 아니라 **진입점**)
  ④"past similar incidents"가 **랜덤 ID 사전순**(`5/N`로 얼어붙는다) ⑤**`Destroy`가 AWS에만
  없어** 파괴 액션이 AWS에선 **AUTO** ⑥**게이트가 테스트마다 Gemini를 과금 호출**(→**D49**,
  288s→39s, ⚠️깨진 테스트 0건) ⑦**optional 임포트 여섯 미선언**(`.[azure]`엔 **LLM조차 없었다**).
  변이 6+6+7+7+8+5+5+5 red(1 생존, 설명됨). ⚠️**렌즈는 여섯째 층 `ExecutorOutput`에서 말랐다** —
  다섯 층에서 다섯 결함, 여섯째는 **범위**(값 사본만 계약에 모음, 동작 변경 0).
- `make check` → **1862~1856**(08-13, **M18이 권위**: GCP/Azure 티어 2가 **원리상 도달 불가**, 결함 셋이 **두 파일에 복사된 채**, 변이 18 red) ·
  **1825**(08-12) · **1789**(08-11, M17) · **1737~1699**(08-09 → **M16**). 상세는 archive가 권위. **여기 남길 함정 셋(08-09)**: 라이브 `gcloud`
  호출(21.97s→0.02s — ⚠️**같은 계열이 08-15에 재발했고 D49가 일반화해 집행**) · 반증 중 **`.pyc`에 속았다** · `spend-watch`는 **터미널을 안 열면 검사도 없다**.
- (이전 baseline — gate **1697 이하** / 07-10~08-09 및 `main` 보호 확인 →
  `docs/archive/{status-baseline-2026-07,progress-2026-08}.md` · `COMPLETED_SUMMARY` M13·M14.)

## Active Focus

> **"무엇이 도는가"는 여기 적지 말 것** — `AGENT_BRIEF` **Snapshot**과 `COMPLETED_SUMMARY`
> **M0~M39**이 권위다. 이 파일은 **무엇을 믿어도 되는가**만 답한다.

**Phase 4 — 4a는 닫혔다(08-30)**: DoD 넷 + **실제 청구 $0.00**(계량 798,331 샘플 · 프리티어가 **`Always Free` 40M/월**이라 §3의 배제 근거는 **전건이 거짓**).
⚠️**믿으면 안 되는 요약 둘**: ⓐ**"어차피 공짜"** — 절벽이 요율→**한도**로 옮겨 앉았을 뿐, 필터 없음 **128배 초과**(허용목록 하중 그대로) ·
ⓑ**"$1.42"** — 720h **연속 가동 가정**, 실측 duty cycle **13%** → 없었어도 **$0.07**(⚠️그 13%는 **그 창의 측정**이고 08-30에 다시 돈다 → Risk 4).
권위 = `docs/plans/2026-08-15-4a-remote-write-allowlist.md` §10 · `…-free-tier-reason-was-inverted.log`(전체 경로는 `NEXT_PLAN`). ⚠️**접기(D50)는 아직 안 됐다**(Risk 4).

**과대 해석 금지(현재 유효한 것만)**
- 스코프·배포 신원 · 이미지 서명 배포 게이트는 전부 **옵트인**(→ Risk 3·6).
- 자격증명이 테넌트-바운드인 건 **온프렘뿐**(→ Risk 10) · 파티션된 읽기 경로는 **둘뿐**.
- `main` 보호는 **게이트 집행**이지 리뷰 집행이 아니다(코드 소유자 리뷰 off → D43).
- **어드미션 미도입** · **CODEOWNERS는 라우팅** · Phase 5는 **경계까지**(UI·PR 생성 없음).

**반복 확인**: 기록된 이유가 진짜 구속 조건이 아닐 때가 많다(M13→M15). 단 **늘 그런 건 아니고** 성립한 쪽이 더 많았는데
**그래도 값은 나왔다** — 08-13에 "안 봤다"를 시험하니 **볼 게 없다는 게 측정으로 바뀌었고** 그 과정에서 **절반만 묻던 가드**가 나왔다.
**"없다"는 어떻게 봤는지까지**(⚠️08-30에 한 리전만 보고 "없다"로 답할 뻔했다 → Risk 4) · **목록을 훑을 땐 목록이 무엇의
그림자인지부터**(Risk 12④ⓐ) · **직전 세션의 Next를 그대로 따라갈 것**(08-13: "덮는 테스트가 있나" → 전부 **모양만 물었다**).
⚠️**열린 항목 자체가 틀릴 수 있다(08-15)**: ⓑ는 "매핑이 없다"였는데 **네 provider 전부 있었고
기록보다 한 달 먼저** 있었다 — **stale이 아니라 쓰일 때부터 틀린 기록**이다. `git log -L`로
**언제부터 있었는지까지** 물어야 stale과 오기를 가른다. 그리고 ⚠️**"선언됐는데 안 읽힌다"는
자동으로 결함이 아니다** — 판단 기준은 **읽는 쪽의 provider 간 비대칭**이지 선언의 고아
상태가 아니다(→ `NEXT_PLAN` 유지 규약의 `provider` 항).

## Open Risks / Gaps

1. **CDK 배포 시 Vercel context 필수** — ⚠️context 미지정 배포가 **실제로 07-11 OIDC provider를 삭제**해 대시보드가 조용히 DEMO FALLBACK으로 강등됐다(07-18 복구). diff/deploy는 반드시 `-c vercelTeamSlug=men16922s-projects -c vercelProjectName=platform-agent`. 로컬 pip 번들링(arm64↔amd64) 주의.
2. **GCP/Azure 인시던트 스토어는 보관 정책이 없다 — 단 "실 데이터 삭제"는 틀렸다** — 코드 갭은
   유효(둘 다 TTL 미설정)지만 **스토어가 아예 없어 지울 데이터 0**(런북 스토어도 마찬가지, 08-13 · **08-30 재확인 여전히 0** — GCP엔 `coffee-menu`, Azure엔 `roadpilot`뿐이고 **둘 다 남의 프로젝트**) → **Phase 4** 선행. 증거 `gcp-azure-retention-nothing-to-delete.log`.
3. **⚠️ 스코프·배포 신원은 집행 가능하지만 옵트인이다(D38·D39)** — **기본값은 미설정**이라
   자격증명 env 없이는 인시던트는 전부 거부, 배포는 ambient(=cluster-admin) —
   **"설정하면 집행되고, 안 하면 조용히 안 된다."** 묻기 `probe_scope_reachability.py`·`make
   deploy-identity-check` · 켜기 `make scope-credentials`·`make deploy-identity`.
   **남은 것**: 배포 신원의 테넌트 구분(결정 5 C/D) · 클라우드 3종은 Risk 10.
4. **세 클라우드 다 기본값이 안심시키는 답을 준다** — 경위 **M16**, 08-12·**08-30 측정 로그가
   권위**(`what-is-actually-billing-2026-08-{12,30}.log`). ⚠️넷째는 클라우드가 아니라 **우리 추정치의
   가정**이었다(4a "수백 시계열" → 실측 52,438). **읽는 법**: 셋 다 `make spend-check`에 박혔고
   **관측 구멍은 GCP 하나**(BQ 내보내기=콘솔 수동, 테이블 0) · ₩20 예산 → **₩28,000** · **측정 자체가
   과금**(CE $0.01/req) · ⚠️**CE 지연 이틀+ — 최근 줄의 0은 지연이지 0이 아니다** · ⚠️**MTD로 "지금"을
   답하지 말 것**(→`--granularity DAILY`; 08-12엔 15배, 08-30 MTD $10.25 중 $9.00이 08-09에 멈춘 것).
   ⚠️**"전 리전 EC2 0대"가 한 번 틀렸다**(정지 t3.medium, 08-16 종료). **지금(08-30 실측)**: AWS
   **$0.0034/일**(도는 EC2 0대 · 바닥 위 유일 항목이 **CE 자기 자신**, 이번 달 $0.64 = 우리 최대 항목) ·
   Azure **₩7,063 전액 남의 ACR**(우리 rg ₩0) · GCP **못 잰다**('₩0'이 아니다 · ~$0.72는 **추정**).
   ⚠️**세는 함정 넷째**: `list-workspaces --region us-east-1`의 `[]`를 "없다"로 읽을 뻔했다 —
   **리전 서비스엔 리전을 돌 것**. ⚠️**4a 접기(D50) 미완**: 워크스페이스 ACTIVE·키 Active(오늘 사용)·
   Prometheus가 **지금도 remote_write**(허용목록 4개 ⇒ **$0.00**) ⇒ **M38의 duty cycle 13%는 그 창의
   측정이지 현재가 아니다**. **대가는 돈이 아니라 장기 키 하나다.**
5. **k3s는 집행하지만 proven 집합엔 없다 — D40으로 닫힘(08-01)** — 집행은 라이브 증명(07-29),
   시맨틱은 미증명. 반복된 "피어 테넌트 부재"는 **참이지만 구속력이 없었다**(진짜 이유는 **순환**
   — 승격하려면 먼저 승격해야). 여는 조건: k3s-lab에 워크로드 → D40 · `docs/plans/2026-08-01-k3s-proven-substrate.md`.
6. **공급망: 생산자·소비자·CI 키리스는 섰고, 어드미션은 업스트림 대기** — **M15가 권위**. `make sign-image` + `image_trust`(배포 직전 거부, **exit 2도 거부**) + CI 키리스까지 라이브.
   **과대 해석 금지**: 배포 게이트는 **옵트인**(`PLATFORM_REQUIRE_SIGNED_IMAGES`)이고 **온프렘 진입점 하나**만 덮는다 · 로컬 `make sign-image`는 **dev 키** · Rekor는 **영구 공개** ·
   **어드미션 미도입**(cosign v3 ↔ policy-controller 저장 위치 불일치, v2는 통과 — 양방향 실증). ⛔**재개 조건 미충족, 08-30 확인**: `sigstore/policy-controller#1406`(번들 스펙)이 **여전히 open**이고 cosign **v3.1.2의 `sign`·`attest` 어디에도 `--new-bundle-format`이 없다**(우리 경로는 `sign --key`).
   ⚠️**우리 게이트는 같은 이미지에 VERIFIED를 준다** = **"검증됨"이 도구마다 다르고 우리는
   못 본다**. 증거 `docs/evidence/cosign-admission-kind-attempt.log` · M15.
7. **TS 타입은 네트워크 데이터를 보증하지 않는다** — 라이브 페이지가 `posture.namespaces.length`로 죽는 동안 `tsc`는 내내 초록이었다(구버전 페이로드). 롤링 중엔 허브가 두 버전을 동시에 서빙 → **푸시 신규 필드는 항상 optional + 폴백**.
8. **PSS restricted 아래에서 애드온 차트는 기본값으로 안 돈다** — 파드는 admission에서 거부되는데 **Argo는 Synced로 보인다**(파드 0개인 채). **새 애드온마다** 렌더된 파드 스펙을 테넌트 ns에 `kubectl apply --dry-run=server`로 던져 API 서버에 직접 물을 것. values 파일은 에러가 아니라 **안 읽히는 방식으로** 실패한다(키 철자가 차트마다 다름).
9. *(해소)* Capsule deprecation(08-02, D41) · GCP 예산 상시 발화 → ₩28,000(08-09) · 리포트 스트림 분리(08-11, M17).
10. **GCP/Azure 자격증명은 아직 테넌트-바운드가 아니다** — 스코프는 **어느 네임스페이스를
   건드릴지**만 정하고 토큰을 테넌트에 안 묶는다(GCP는 프로젝트 전역 신원 하나, Azure는 ARM에서 클러스터 admin kubeconfig). 자격증명 자체가 경계인 건 **온프렘뿐** → Phase 4. ⚠️**08-30부터 Azure 경로가 실제로 열려 있다**(M39 배선) — 오늘 대상 자원이 0개라 blast radius가 0인 것이지 **경계가 생긴 게 아니다**.
11. **Dashboard dependency audit — 해소됨(08-30). `npm audit` 0건.** ⚠️**"upstream 대기"로 다시 닫지 말 것** — 그 기록이 **세 군데 다 틀렸다**: 적힌 *"PostCSS moderate 2건 · 패치 없음 · 빌드타임이라 런타임 위험 낮음"* 대신 실측은 **critical 2 · high 6**이고 **런타임**이었다(`next-auth`가 *"auth checks **fail open**"*인데 **승인 UI 포함 런타임 8파일**에 있었다 · next는 **미들웨어 우회·SSRF·내부 Server Function 노출** · PostCSS조차 **high**). ⚠️기록의 반증 실험이 **`--force` 하나뿐**이었고 그 답을 항목 전체의 성질로 썼다(08-18 "러너 하나의 성질"과 같은 모양). 조치: **Tier A** `npm audit fix`(critical 2→0, lockfile만) → **Tier B** `next 16.2.10→16.3.3`+`eslint-config-next`(**총 8→0**). 검증: `tsc` 통과 · `build` exit 0 · **라우트 매니페스트 17개 before/after 동일** · **eslint 41건이 main과 완전히 같다**(새 소견 0 — worktree로 실측). 증거 `docs/evidence/the-dashboard-audit-record-described-a-different-risk.log`.
12. **게이트의 초록에는 조건이 붙는다 — 일곱이 같은 계열(2026-08-08~15)** —
   ①**시간**: 픽스처가 절대 시각을 하드코딩하고 생산자가 살아 있는 시계로 창을 걸면 통과는
   **달력이 움직이기 전까지만** 참이다(07-29 픽스처 → 08-05 만료). ②**환경**: 게이트가
   **선언되지 않은 패키지** 위에서 통과하고 있었다(새 클론은 아무도 통과 못 함) — CI가
   잡았고 로컬에선 원리상 안 드러난다(`requires-python = ">=3.11"`은 **08-30에 재서 거짓** — fresh 3.11은 **2 failed**, 같은 의존성 fresh 3.13은 초록 ⇒ 인터프리터 탓이고 선언 셋을 3.13으로 맞췄다. ⚠️같은 스윕에서 **`[tool.mypy] strict = true`를 아무도 안 돈다**는 것도 나왔다 — 253 errors/88 files 떨어져 있어 지우지 않고 **주석+가드**로 남겼다). 역방향도 실측: **skip은 실패가 아니라서 검사 안 하는 게이트와 통과한
   게이트가 같은 색**이다 → **게이트 숫자엔 날짜와 잰 기계를 붙일 것.**
   ⚠️**08-30에 여덟째**: 문서화된 **`make dev-up`이 새 클론에서 못 돌았다** — `.venv-mlx`를 **만드는 타깃이 없었고** 둘은 `nohup`이라 **조용히** 실패했다(`mlx-setup` 추가 + 세 지점 사전검사, +5).
   ③**하중**: **행복 경로만 태운 가드는 하중을 받지 않는다**(안전망을 지워도 14개가 초록)
   → **새 가드는 지워 보고 red를 확인할 것.** ⚠️**둘째 얼굴**(08-12) — 분기를 **깨져야만
   열리는 플래그**로 걸면 그 분기는 *원리상 도달 불가*다(에스컬레이션 6스텝을 아무도 안
   걸었다). **조건 축은 값을 넣어 명시적으로 걸 것.** ④**관측 지점 — 상세는 M17이 권위**:
   **가드는 독자가 읽는 그 물건에 대고 물을 것.** 결론 다섯 — ⓐ**결함을 그 그림자로 세지 말
   것** · ⓑ**"stderr 금지"가 아니다**(독자가 파서면 의무가 거꾸로) · ⓒ**절반만 묻는 가드** ·
   ⓓ**문이 넷이다**(`print`·stderr 외에 **로깅**과 **잡히지 않은 예외**) · ⓔ**"클러스터가
   필요하다"는 잰 게 아니었다**(**두 번**, **실패 경로는 공짜였다** — 합 6건). ⚠️**세 번째
   (08-13)엔 변명이 참**이었고 **대신 ⓒ가 재발했다** — **시험은 결함이 아니라 범위를 줄 때도
   값이 있다.** 남은 건 `slack_live_approval`.
   ⑤**가드 자신의 값과 하네스(M18)**: **기본값과 같은 값을 고른 픽스처는 가드가 아니다** ·
   **초록으로 안 돌아오는 복구는 복구가 아니다** · **한 블록 두 파일**이면 **provider 간
   일치를 묻는 가드**를 같이 둘 것. ⚠️**⑥형제 집합(08-13에 세 번, 08-15에 둘 —
   M18~M20)**: 가드가 **형제 중 하나만 순회**하면 나머지는 안 물어진 채다. 셋째는 **그걸
   찾으려고 만든 스윕 안에서**, 넷째는 **두 파일 중 한쪽만 import가 되돌아가**, 다섯째는
   **네 어댑터를 정확히 열거한 도크스트링 아래 `aws.analyzer`만 임포트**한 가드였다 →
   **세는 순간 전부 세고, ⚠️산문이 참이어도 임포트 줄이 범위다.**
   ⑦**변이 하네스 자신(08-15에 다섯, 08-30에 여섯째)**: ⚠️**복구 확인이 `.pyc`에 속았다**(디스크는 복구됐는데 하네스가 red를 답했다 — `__pycache__` 삭제 후 초록) ⇒ **복구 확인 앞에 캐시를 지울 것** · 틀린 파일에 물었다 · **문법을 깬 변이**를 red로 셌다 ·
   **없는 파일을 적어 `no tests ran`**을 red로 셌다 → **기준선을 먼저 찍고 "몇 건이 죽었는지"까지**
   · **변이·실행·복구는 한 스크립트에**, 단 **복구는 프로세스 밖에도**(디스크 백업이 2회 구제).
   증거는 **M17~M20이 각각 자기 로그를 가리킨다**(`docs/evidence/`).
- (그 밖의 해소 이력은 `PROGRESS_LOG`/`docs/archive/` 참조.)
