# STATUS Active Focus / Open Risks — 옮겨 온 상세 (2026-09-02)

> `docs/STATUS.md`가 문자 예산(11,000)을 넘겨 **상세를 여기로 옮겼다. 삭제가 아니라 이동**이다.
> STATUS에는 **무엇을 믿어도 되는가**(주장 한 줄 + 재개 조건)만 남기고, **왜 그렇게 아는가**
> (측정·경위·⚠️/⛔ 문장)는 이 파일이 권위다. 위험 번호는 STATUS와 **같은 번호**를 쓴다 —
> 번호가 갈리면 두 문서가 다른 목록이 되고, 그게 이 레포가 반복해서 고쳐 온 실패다.
> 각 항목의 최종 권위는 그 안에 적힌 **M번호**와 `docs/evidence/*`다.

---

## Active Focus — Phase 4 / 4a (08-30 닫힘, 09-01 접힘)

DoD 넷 + 실제 청구 **$0.00**(프리티어 `Always Free` 40M) → 워크스페이스·IAM 사용자·키 **셋 다 삭제**.
⚠️**믿으면 안 되는 요약 셋**: **"어차피 공짜"**(절벽이 요율→**한도**로 옮겼을 뿐, 필터 없음 **128배**) ·
**"$1.42"**(720h 연속 가동 **가정**이었다 — 실측 청구는 **$0.00**이고 사유는 `Always Free` 40M/월) ·
**"$0.00이라 접었다"**(대가는 **장기 키**였다). ⚠️**D48은 안 죽었다**.
권위 = 계획서 **§10·§11** · **M38·M40** · **D50 Folded**. **다시 열지 말 것.**

## Active Focus — 반복 확인

기록된 이유가 진짜 구속 조건이 아닐 때가 많다(M13→M15). 단 **늘 그런 건 아니고** 성립한 쪽이 더 많았는데
**그래도 값은 나왔다** — 08-13에 "안 봤다"를 시험하니 **볼 게 없다는 게 측정으로 바뀌었고** 그 과정에서
**절반만 묻던 가드**가 나왔다. **"없다"는 어떻게 봤는지까지**(⚠️08-30에 한 리전만 보고 "없다"로 답할 뻔했다
→ Risk 4) · **목록을 훑을 땐 목록이 무엇의 그림자인지부터**(Risk 12④ⓐ) · **직전 세션의 Next를 그대로
따라갈 것**(08-13: "덮는 테스트가 있나" → 전부 **모양만 물었다**).
⚠️**열린 항목 자체가 틀릴 수 있다(08-15)**: ⓑ는 "매핑이 없다"였는데 **네 provider 전부 있었고
기록보다 한 달 먼저** 있었다 — **stale이 아니라 쓰일 때부터 틀린 기록**이다. `git log -L`로
**언제부터 있었는지까지** 물어야 stale과 오기를 가른다. 그리고 ⚠️**"선언됐는데 안 읽힌다"는
자동으로 결함이 아니다** — 판단 기준은 **읽는 쪽의 provider 간 비대칭**이지 선언의 고아
상태가 아니다(→ `NEXT_PLAN` 유지 규약의 `provider` 항).

## Risk 1 — CDK 배포 시 Vercel context 필수

⚠️context 미지정 배포가 **실제로 07-11 OIDC provider를 삭제**해 대시보드가 조용히 DEMO FALLBACK으로
강등됐다(07-18 복구). diff/deploy는 반드시
`-c vercelTeamSlug=men16922s-projects -c vercelProjectName=platform-agent`.
로컬 pip 번들링(arm64↔amd64) 주의.

## Risk 2 — GCP/Azure 보관 정책

**"실 데이터 삭제"는 틀렸다** — 코드 갭은 유효(둘 다 TTL 미설정)지만 **스토어가 아예 없어 지울 데이터 0**
(런북 스토어도 마찬가지 · **08-30 재확인 여전히 0** — GCP엔 `coffee-menu`, Azure엔 `roadpilot`뿐이고
**둘 다 남의 프로젝트**) → **Phase 4** 선행. 증거 `docs/evidence/gcp-azure-retention-nothing-to-delete.log`.

## Risk 3 — 스코프·배포 신원은 집행 가능하지만 옵트인 (D38·D39)

**기본값은 미설정**이라 자격증명 env 없이는 인시던트는 전부 거부, 배포는 ambient(=cluster-admin) —
**"설정하면 집행되고, 안 하면 조용히 안 된다."** 묻기 `probe_scope_reachability.py`·`make
deploy-identity-check` · 켜기 `make scope-credentials`·`make deploy-identity`.
**남은 것**: 배포 신원의 테넌트 구분(결정 5 C/D) · 클라우드 3종은 Risk 10.

## Risk 4 — 세 클라우드 다 기본값이 안심시키는 답을 준다

경위 **M16**, 측정 로그가 권위(`what-is-actually-billing-2026-08-{12,30}.log` ·
**`the-measurement-is-the-bill.log`(09-01)**).
⚠️넷째는 클라우드가 아니라 **우리 추정치의 가정**이었다(4a "수백 시계열" → 실측 52,438).
**읽는 법**: 셋 다 `make spend-check`에 박혔고 · ₩20 예산 → **₩28,000** · ⚠️**CE 지연 이틀+ —
최근 줄의 0은 지연이지 0이 아니다** · ⚠️**MTD로 "지금"을 답하지 말 것**(→`--granularity DAILY`).
⚠️**"전 리전 EC2 0대"가 한 번 틀렸다**(정지 t3.medium, 08-16 종료).
**지금(09-01 실측)**: AWS 바닥 **$0.0034/일**(도는 EC2 0대). ⚠️**서비스별로 가르니 결론이 바뀐다** —
08-20~09-01 총 **$0.2380** 중 **Cost Explorer가 $0.2000 = 84%**이고 진짜 인프라는 **월 ≈$0.09**다.
**관측이 관측 대상을 만든다 — 조회 남발 금지는 산수다.** · Azure **쟀다 — 기록된 결론이 뒤집혔다**:
서버가 **`clienttype-retry-after`로 대기 시간을 말한다**(5→**40**; 고갈된 버킷은 `clienttype` 하나).
40을 지키니 **200** — ACR **₩76.67**·Cosmos DB ₩0(전액 남의 것). ⚠️*"재시도가 답이 아니었다"*는
**간격이 요구값 아래**였던 것(20초<40초) — **고정 간격 재시도는 영구 실패와 구분되지 않는다**
(`az rest`는 헤더를 버린다). · GCP **아직 답이 없다**(01:53Z 생성·14:08Z에도 **0행** — ⚠️**'₩0'이 아니다**,
판정은 **09-02 01:11Z 이후**). ⚠️**세 답의 모양이 다르다 — 하나로 요약하지 말 것.**
⚠️**세는 함정 다섯**: 리전 한정 조회의 `[]` · `AccessDenied` · 429 · **`bq show` 실패** —
**전부 "없다"가 아니다**, 그리고 ⚠️**빈 테이블도 측정이 아니다** ·
⚠️**로그 창이 사건보다 넓으면 지나간 실패를 현재로 읽는다**(09-01, remote_write).
⛔**4a 접기(D50)는 2026-09-01에 완료**(셋 다 삭제, 8리전 스윕 0 — M40) ⇒ **AMP는 지출·관측에서 빠진다.**
⚠️**09-01 후속**: kind를 띄우니 **삭제된 워크스페이스로 계속 403**이었다 — **값 파일과 배포된 릴리스는
다르다**(helm rev 5→6). ⚠️**`--reuse-values`는 지우려는 그 블록을 되살린다.**

## Risk 5 — k3s는 집행하지만 proven 집합엔 없다 (D40, 08-01 닫힘)

집행은 라이브 증명(07-29), 시맨틱은 미증명. 반복된 "피어 테넌트 부재"는 **참이지만 구속력이 없었다**
(진짜 이유는 **순환** — 승격하려면 먼저 승격해야). 여는 조건: k3s-lab에 워크로드 → D40 ·
`docs/plans/2026-08-01-k3s-proven-substrate.md`.

## Risk 6 — 공급망: 어드미션만 업스트림 대기

**M15가 권위**. `make sign-image` + `image_trust`(배포 직전 거부, **exit 2도 거부**) + CI 키리스까지 라이브.
**과대 해석 금지**: 배포 게이트는 **옵트인**(`PLATFORM_REQUIRE_SIGNED_IMAGES`)이고 **온프렘 진입점 하나**만
덮는다 · 로컬 `make sign-image`는 **dev 키** · Rekor는 **영구 공개** · **어드미션 미도입**
(cosign v3 ↔ policy-controller 저장 위치 불일치, v2는 통과 — 양방향 실증).
⛔**재개 조건 미충족, 08-30 확인**: `sigstore/policy-controller#1406`(번들 스펙)이 **여전히 open**이고
cosign **v3.1.2의 `sign`·`attest` 어디에도 `--new-bundle-format`이 없다**(우리 경로는 `sign --key`).
⚠️**우리 게이트는 같은 이미지에 VERIFIED를 준다** = **"검증됨"이 도구마다 다르고 우리는 못 본다**.
증거 `docs/evidence/cosign-admission-kind-attempt.log` · M15.

## Risk 7 — TS 타입은 네트워크 데이터를 보증하지 않는다

라이브 페이지가 `posture.namespaces.length`로 죽는 동안 `tsc`는 내내 초록이었다(구버전 페이로드).
롤링 중엔 허브가 두 버전을 동시에 서빙 → **푸시 신규 필드는 항상 optional + 폴백**.

## Risk 8 — PSS restricted 아래에서 애드온 차트는 기본값으로 안 돈다

파드는 admission에서 거부되는데 **Argo는 Synced로 보인다**(파드 0개인 채). **새 애드온마다** 렌더된 파드
스펙을 테넌트 ns에 `kubectl apply --dry-run=server`로 던져 API 서버에 직접 물을 것. values 파일은 에러가
아니라 **안 읽히는 방식으로** 실패한다(키 철자가 차트마다 다름).

## Risk 9 — (해소)

Capsule deprecation(08-02, D41) · GCP 예산 상시 발화 → ₩28,000(08-09) · 리포트 스트림 분리(08-11, M17).

## Risk 10 — GCP/Azure 자격증명은 아직 테넌트-바운드가 아니다

스코프는 **어느 네임스페이스를 건드릴지**만 정하고 토큰을 테넌트에 안 묶는다(GCP는 프로젝트 전역 신원 하나,
Azure는 ARM에서 클러스터 admin kubeconfig). 자격증명 자체가 경계인 건 **온프렘뿐** → Phase 4.
⚠️**08-30부터 Azure 경로가 실제로 열려 있다**(M39 배선) — 오늘 대상 자원이 0개라 blast radius가 0인 것이지
**경계가 생긴 게 아니다**.

## Risk 11 — Dashboard dependency audit (08-30 해소, `npm audit` 0건)

⚠️**"upstream 대기"로 다시 닫지 말 것** — 그 기록이 **세 군데 다 틀렸다**: 적힌 *"PostCSS moderate 2건 ·
빌드타임"* 대신 실측은 **critical 2 · high 6**이고 **런타임**이었다(`next-auth`가 *"auth checks **fail
open**"*인데 **승인 UI 포함 런타임 8파일**에). ⚠️반증 실험이 **`--force` 하나뿐**이었고 그 답을 항목
전체의 성질로 썼다. 조치·검증 상세는 **증거 로그가 권위**
(`docs/evidence/the-dashboard-audit-record-described-a-different-risk.log`).

## Risk 12 — 게이트의 초록에는 조건이 붙는다 (아홉이 같은 계열, 2026-08-08~09-01)

⑨**워크플로가 안 도는 것도 초록으로 보인다(09-01, M43)**: `gate.yml`이 `pull_request: branches: [main]`이라
**스택 PR엔 job이 안 돌았고**, `gh pr checks`엔 **`check` 행이 아예 없어** 통과와 구분이 안 됐다 — 실제로
PR #58을 그렇게 읽었다. ⛔**`main`은 무방비였던 적 없다**(보호가 `check`를 요구). **②의 한 층 위**: 거기선
건너뛴 **테스트**였고 여기선 안 돈 **워크플로**다. 필터를 지워 **모든 PR이 게이트를 받는다**.
①**시간**: 픽스처가 절대 시각을 하드코딩하고 생산자가 살아 있는 시계로 창을 걸면 통과는 **달력이 움직이기
전까지만** 참이다(07-29 픽스처 → 08-05 만료). ②**환경**: 게이트가 **선언되지 않은 패키지** 위에서 통과하고
있었다 — CI가 잡았고 로컬에선 원리상 안 드러난다(`requires-python = ">=3.11"`은 **08-30에 재서 거짓**:
fresh 3.11은 **2 failed**, 같은 의존성 fresh 3.13은 초록 ⇒ 선언 셋을 3.13으로 맞췄다. ⚠️같은 스윕에서
**`[tool.mypy] strict = true`를 아무도 안 돈다**도 나왔다 — 253 errors/88 files라 **주석+가드**로 남겼다).
역방향도 실측: **skip은 실패가 아니라서 검사 안 하는 게이트와 통과한 게이트가 같은 색**이다 →
**게이트 숫자엔 날짜와 잰 기계를 붙일 것.** ⚠️**08-30에 여덟째**: 문서화된 **`make dev-up`이 새 클론에서
못 돌았다**(`.venv-mlx`를 만드는 타깃이 없었고 둘은 `nohup`이라 **조용히** 실패 — `mlx-setup` 추가 +
세 지점 사전검사, +5).
③**하중**: **행복 경로만 태운 가드는 하중을 받지 않는다**(안전망을 지워도 14개가 초록) → **새 가드는 지워
보고 red를 확인할 것.** ⚠️**둘째 얼굴**(08-12) — 분기를 **깨져야만 열리는 플래그**로 걸면 *원리상 도달
불가*다 ⇒ **조건 축은 값을 넣어 명시적으로 걸 것.**
④**관측 지점 — M17이 권위**: **가드는 독자가 읽는 그 물건에 대고 물을 것.** ⓐ**결함을 그 그림자로 세지
말 것** · ⓑ**"stderr 금지"가 아니다** · ⓒ**절반만 묻는 가드** · ⓓ**문이 넷** · ⓔ**"클러스터가 필요하다"는
잰 게 아니었다**(합 6건). ⚠️**ⓐ가 09-01에 두 번 재발**(M40·M41): 합성 표를 `violations != []`로만 물어
**와일드카드 검사를 지워도 초록**이었고, `"mlx-lm" in Makefile`은 **주석으로 통과**했다 ⇒ **위반에 코드를
붙이고 케이스가 어느 규칙이 물어야 하는지 지정할 것.** 남은 건 `slack_live_approval`.
⑤**가드 자신의 값과 하네스(M18)**: **기본값과 같은 값을 고른 픽스처는 가드가 아니다** · **초록으로 안
돌아오는 복구는 복구가 아니다** · **한 블록 두 파일**이면 **provider 간 일치를 묻는 가드**를 같이 둘 것.
⚠️**⑥형제 집합(08-13에 셋, 08-15에 둘, 09-01에 하나 — M18~M20·M42)**: 가드가 **형제 중 하나만 순회**하면
나머지는 안 물어진 채다. 셋째는 **그걸 찾으려고 만든 스윕 안에서**, 09-01의 것은 **형제를 세는 가드 자신**
(파일명이 `both_tools`)이었다 → **세는 순간 전부 세고, 산문이 참이어도 임포트 줄이 범위다.**
⑦**변이 하네스 자신(08-15에 다섯, 08-30·09-01에 더)**: **복구 확인이 `.pyc`에 속았다** ⇒ **앞에 캐시를
지울 것** · **틀린 파일에 물었다**(09-01에 두 번 더) · **변이를 적용하지 않고 변이를 쟀다**(09-01) →
**변이 적용부터 확인하고, 기준선을 먼저 찍을 것** · **변이·실행·복구는 한 스크립트에**.
증거는 **M17~M20이 각각 자기 로그를 가리킨다**.
