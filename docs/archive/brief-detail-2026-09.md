# AGENT_BRIEF — 옮겨 온 상세 (2026-09-02)

> `docs/AGENT_BRIEF.md`가 문자 예산의 90%를 넘겨 **상세를 여기로 옮겼다. 삭제가 아니라 이동**이다
> (예산 숫자 자체는 `.claude/harness-config.json` 한 곳에만 산다 — 네 번째 표기가 드리프트다, M19).
> 브리프에는 **무엇을 믿고 시작하는가**(주장 한 줄 + 재개 조건)만 남기고, **왜 그렇게 아는가**
> (경위·측정·⚠️/⛔ 문장)는 이 파일이 권위다. 아래 블록은 옛 AGENT_BRIEF에서 **한 글자도 고치지
> 않고 옮긴 것**이다 — 09-02 STATUS 압축이 *"압축은 재작성이고 재작성은 조용히 지운다"*를 남겼으니,
> 여기서도 줄을 손이 아니라 **스크립트가 복사**했다(줄은 줄 번호로, 문단은 **마커로** 잘랐다).
> ⚠️**브리프의 줄은 `> ` 인용 접두를 달고 있었고 그것까지 그대로다** — 원문 대조를 위해서다.
> 각 항목의 최종 권위는 그 안에 적힌 **M번호**·**D번호**와 `docs/evidence/*`다.

---

## ▶ NEXT SESSION — 2026-09-01에 닫힌 세 결함 (권위: `PROGRESS_LOG` 최상단 · M46·M47)

⛔**09-01에 세 결함이 닫혔다 — 상세는 `PROGRESS_LOG` 최상단과 M46이 권위, 여기 다시 적지 말 것**: 빈 테이블이 MEASURABLE로 읽힘 · **인쇄되던 `bq` 명령이 돌지 않음**(콜론·`--project_id`·credits — ⚠️**인쇄된 지시도 주장이고 한 번도 실행된 적이 없었다**) · **Azure 429의 기록된 결론이 뒤집힘**(⚠️**재시도는 두 번 다 답이었다** — 서버가 `clienttype-retry-after`로 **40초**를 요구하는데 20초에 물었다; **고정 간격 재시도는 영구 실패와 구분되지 않는다**. 쟀다 = ACR **₩76.67**).

## 무과금으로 이어서 할 것 — 목록은 비었다 (닫힌 항목 여섯)

> **무과금으로 이어서 할 것**: ⛔**목록이 비었다** — 08-17에 셋 다 닫혔다: **ⓐ**=현행 유지(M35, 두 선택지가 **대칭이 아니었다**) · **ⓒ**=측정하고 고쳤다(M36+M37 — 세 provider가 같은 인시던트에 **같은 답**) · **`rollback_release`**=추천 목록은 **현행 유지**이고 진짜 문제는 목록이 아니라 **티어 2가 조건 평가 없이 추천에서 액션을 만든 것**이었다(M37). ⚠️**"비었다"는 목록의 상태지 사실이 아니다**(아래 "소진은 여섯 번 틀렸다") · ⛔**"런북 walk ②"는 닫혔다**(08-17, M34 — `severity` 축은 M33이 닫았고 e2e walk의 P2 고정은 **결함이 아니라 범위**다: 카탈로그가 쓰는 조건 키는 `previous_step_failed` **하나뿐**. 대신 **셋째 형식 `provider`가 세 walk 전부에서 안 물어지고 있었다** — 가드 +6) ·
> ⛔`slack_live_approval`도 **닫혔다**(08-16 — 데모 선행은 틀린 기록이었다). ⛔**`triggered_at`·`metric_name`은 닫혔다**(08-15 전수 스윕, M21 증거 §8 — 결함이 아니라 **범위**, 8필드 중 닿는 건 `reason` 하나). **다시 열지 말 것.**

## 직전 세션 → 2026-09-01 (일곱 묶음) + 거기서 배운 것 둘

> 직전 세션 → **09-01**: 일곱 묶음(**PR #57·#59·#60·#61·#62·#63 + 인시던트 기본값**) — 08-30 묶음 랜딩 · **4a 접기**(M40) · **`mlx-lm`**(M41) · **셋째 형제 mypy**(M42) · **모든 PR이 게이트를 받는다**(M43) · **계약이 빠뜨린 메서드**(M44) · **시그니처↔본문 불일치**(M45) · **빈 테이블이 측정으로 읽혔다**(M46). ⛔**상세를 여기 다시 적지 말 것** — `PROGRESS_LOG` 상단과 `docs/archive/progress-2026-08.md`가 권위다. ⚠️**배운 것 둘**: **`| head -N`이 찾던 파일을 잘랐다**(docs/가 infra/보다 먼저 정렬돼 "레포엔 없다"고 쓸 뻔 — 세는 함정은 도구만이 아니다) · **`AccessDenied`는 부재의 증거가 아니다**(권한이 없어 나온 답이라 대상이 살아 있어도 같다 — 429가 *"'0'이 아니다"*였던 것과 같은 모양).
