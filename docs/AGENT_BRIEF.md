# AGENT_BRIEF — platform-agent

최종 갱신: 2026-08-18

> ▶ NEXT SESSION: **첫 행동 = Azure executor 디스패치**(승인 사안 — 고치면 **Phase 3② 면제도**
> **같이 지워야 하고, 가드가 그걸 red로 잡는다**). ⛔**4a는 닫혔다(08-30)** — 계획서 **§10이 권위**:
> 실제 청구 **$0.00**(계량 798,331 샘플)이고 사유는 **AMP 프리티어가 `Always Free` 40M/월**이라
> §3의 배제 근거가 **전건이 거짓**이었다는 것. ⚠️**"어차피 공짜"로 요약 금지** — 절벽이 요율→**한도**로
> 옮겨 앉았을 뿐이고 필터 없음은 **128배 초과**다. ✅대시보드 취약점은 **0건**이다(08-30, Risk 11 — ⚠️"기록이 세 군데 틀렸다"를 지우지 말 것).
> ✅**DoD 넷은 다 닫혔다**(08-17, M37) — 관리형은 **매니페스트를 내지 않고** read model이 그 부재를 설명한다(`applicable=False`·sync **n/a**). **새 매니페스트 종류는 발명하지 않았다.**
> ⚠️**$0 선행**: BQ 결제 내보내기만 남았다(콘솔 수동 → `docs/GCP_BILLING_EXPORT_SETUP.md`,
> 확인은 `make spend-check`).
> **무과금으로 이어서 할 것**: ⛔**목록이 비었다** — 08-17에 셋 다 닫혔다: **ⓐ**=현행 유지(M35, 두 선택지가 **대칭이 아니었다**) · **ⓒ**=측정하고 고쳤다(M36+M37 — 세 provider가 같은 인시던트에 **같은 답**) · **`rollback_release`**=추천 목록은 **현행 유지**이고 진짜 문제는 목록이 아니라 **티어 2가 조건 평가 없이 추천에서 액션을 만든 것**이었다(M37). ⚠️**"비었다"는 목록의 상태지 사실이 아니다**(아래 "소진은 여섯 번 틀렸다") · ⛔**"런북 walk ②"는 닫혔다**(08-17, M34 — `severity` 축은 M33이 닫았고 e2e walk의 P2 고정은 **결함이 아니라 범위**다: 카탈로그가 쓰는 조건 키는 `previous_step_failed` **하나뿐**. 대신 **셋째 형식 `provider`가 세 walk 전부에서 안 물어지고 있었다** — 가드 +6) ·
> ⛔`slack_live_approval`도 **닫혔다**(08-16 — 데모 선행은 틀린 기록이었다). ⛔**`triggered_at`·`metric_name`은 닫혔다**(08-15 전수 스윕, M21 증거 §8 — 결함이 아니라 **범위**, 8필드 중 닿는 건 `reason` 하나). **다시 열지 말 것.**
>
> ⚠️ `main` 보호 — 직접 push 불가(소유자 포함), **PR + CI 통과로만 병합**(D43).
> ⚠️ 초록에는 조건이 붙는다(Risk 12, 이제 **일곱**): **새 가드는 지워 보고 red를 확인**(③) · **skip은 실패가 아니다** — 게이트 숫자엔 **잰 기계**까지 붙일 것(②) · **가드는 독자가 읽는 그 물건에 대고 물을 것**(④ — **결함을 그 그림자로 세지 말 것**, **경고는 주장만 묻지 말고 지시까지**, **문은 넷**) · ⑤**가드 자신도 틀린다**(08-13: 픽스처로 고른 값이 **틀린 기본값과 같아** 결함을 통과시켰다 · 변이 하네스가 `git checkout --`로 **커밋 안 된 고침을 날리고** 있었다 — **초록으로 안 돌아오는 복구는 복구가 아니다**) · ⑦**변이 결과는 어디에 물었는지까지 말할 것**(08-15: **틀린 테스트 파일**에 물어 "가드 없음"으로 읽었는데 전체 스위트로는 red였다 · **변이·실행·복구는 한 스크립트 안에** 둘 것).
> ⚠️ **"소진"은 이제 여섯 번 틀렸다** — 가장 값싼 다음 수는 대개 **직전 세션이 이미 적어 뒀다**. ⚠️**목록에 적힌 항목 자체가 틀릴 수도 있다**(08-15: ⓑ는 "매핑 없음"이었는데 네 provider 전부 있었고 **기록보다 한 달 먼저** 있었다) — **틀린 항목도 값이 난다**(시험하다 옆의 진짜 결함이 나왔다). 단 **`git log -L`로 "언제부터"까지** 물어야 stale과 오기를 가른다.
> ⚠️ **"안 봤다"를 시험하면 결함이 아니라 범위가 나올 때도 있다**(08-13에 두 번). ⚠️**형제 집합은 세는 순간 전부 셀 것** — 08-13에 세 번(**세 번째는 그걸 찾으려고 만든 스윕 안에서**), **08-15에 둘 더**(한쪽만 import가 되돌아가 죽어 있었다 · **네 어댑터를 열거한 도크스트링 아래 `aws.analyzer`만 임포트**한 가드 — ⚠️**산문이 참이어도 임포트 줄이 범위다**). ⚠️**"선언됐는데 안 읽힌다"는 자동으로 결함이 아니다** — 기준은 **읽는 쪽의 provider 간 비대칭**이다(`provider` 필드를 읽기 시작하면 GCP/Azure는 전부 폴백으로 떨어진다). ⚠️**읽는 쪽을 고쳤으면 쓰는 쪽도 물을 것**(08-17): 조건을 읽게 만든 다음 물으니 **검증기가 `steps` 안을 아예 안 봤다** — 오타 난 조건 키는 평가에서 `True`가 되어 **에스컬레이션이 무조건 실행**된다(같은 결함, 반대쪽 문). ⚠️**검증기가 둘이면 느슨한 쪽이 실제 경로다** — 엄격한 `validate_capability_runbook`은 **테스트만 부르고 있었다**.
> ⚠️ **추정표는 어느 칸이 측정이고 어느 칸이 가정인지 표시할 것**(08-15, 4a): 정가는 맞았는데 **안 잰 시계열 수**가 총액을 100배 지배했다 — **가정이 지배하는 추정은 추정이 아니라 그 가정이다.** 그리고 **권위 문서가 틀리면 복제본이 그것을 사실로 굳힌다**(진입점 3곳이 "$5"를 복제해 승인까지 갔다 — **복제 금지 규약이 겨냥한 실패**).
> 직전 세션 → **08-30**은 여덟이다. **⑧"SDK가 90%+ 상이"를 쟀다** — SDK 접촉은 **3~5%**뿐이고, `detector`는 규약대로 동일 블록 **0줄**인데 `analyzer` **51%**·`decision` **56%**가 **글자 그대로 두 벌**이다(=SDK가 아니라 **계약**). 추출은 승인 사안이라 안 하고 **바이트 동일 7쌍을 표로 박았다**(+8). ⚠️**유사도를 의미로 읽지 말 것**: 가장 달라 보인 `_determine_mode`(27.4%)는 D48을 집행하는데 셋이 **의미상 동일**했다. ⚠️**진짜 비대칭 하나는 이미 닫혀 있었다**(AWS엔 없는 휴리스틱 폴백이 P1→AUTO로 갈 뻔하지만 `reconciliation`이 `confidence<0.5`로 내린다). ⚠️**내 grep이 거짓 음성을 냈다** — `git grep -E`의 `\s`는 조용히 0건이다(세는 함정 **셋째**). **⑦문서화된 `make dev-up`이 새 클론에서 못 돌았다** — `.venv-mlx`를 **만드는 타깃이 0개**였고(손으로 만든 venv였다: `pydantic-ai-slim`이 없어 `.[onprem]` 산물도 아니다) 세 지점 중 **둘이 `nohup`이라 조용히** 실패했다 → `make mlx-setup` + 사전검사(+5, 변이 4종 red). ⚠️**내가 만든 가드가 한 번 틀렸다**: Make는 레시피 줄마다 셸이 달라 `exit 0`이 다음 줄을 못 막았고 pip가 기존 venv에 그대로 돌았다 — **뒤따르는 것을 막지 못하는 검사는 검사가 아니다**. **⑥두 번 "미검증"이라 적어 둔 주장을 쟀다 — 거짓이었다**: fresh 3.11은 **2 failed**이고, ⚠️**결론 전에 교란 요인을 지웠다**(3.11 venv가 최신 패키지를 받아서 인터프리터 탓인지 몰랐다 → **fresh 3.13이 같은 버전으로 해석되고 초록**) ⇒ 선언 셋(`requires-python`·ruff·mypy)을 **3.13**으로 맞췄다. ⚠️좁히기 전에 **워크플로 둘의 파이썬이 다른 것**을 먼저 봤다(`sign-image.yml`은 3.11 — `pip install` 0개라 무영향). 곁가지로 **`[tool.mypy] strict = true`를 아무도 안 돈다**(253 errors)는 것도 나왔고, 지우지 않고 **주석+가드**로 남겼다. **⑤Tier B 수행 — 대시보드 취약점 8→0**(`next 16.3.3`, 소스 변경 0): ⚠️**"조용히 실패하는 쪽"을 겨눴다** — `build` exit 0만 보지 않고 **라우트 매니페스트 17개를 before/after로 대조**했고(동일), `npm run lint` 41건도 **기존 것이라 가정하지 않고** `git worktree`로 main을 따로 깔아 재서 **완전히 동일**함을 확인했다(**새 소견 0**). ⚠️Risk 11은 해소로 바꾸되 **"기록이 세 군데 틀렸다"는 남겼다** — 결론이 닫혔다고 왜 틀렸는지가 지워지면 같은 방식으로 다시 닫힌다. **④"업스트림 대기" 둘의 재개 조건을 쟀다**: `.[azure]`는 **설치가 열렸고**(150초 타임아웃 → **31.5초 성공**, 1.16.0) **심볼은 여전히 없다**(`AzureOpenAI*Client` 클래스 **0개**, 그 이름은 **업스트림 docstring 한 줄**에만 — 대체가 없으니 추측은 발명) · ⚠️**Risk 11은 기록이 세 군데 다 틀렸다**(moderate 2·빌드타임·패치 없음 → **critical 2·high 6·런타임**, `next-auth`가 **auth fail open**인데 **승인 UI 포함 8파일**에 있다; 반증 실험이 **`--force` 하나뿐**이었다) → **Tier A 적용**(critical 2→0, lockfile만, build 통과), **Tier B(`next@16.3.3`)는 결정 사안**. **③승인 항목의 근거를 쟀다**(코드 변경 0): Azure "순수 잠재"의 *"Cosmos 없다"*가 **틀렸고**(`cosmos-roadpilot`이 그 측정보다 **한 달 먼저** 있었다 — **stale이 아니라 오기** · **남의 프로젝트**) 동시에 **러너엔 Cosmos 액션이 없다** ⇒ 근거가 **손으로 적은 셋 → 러너 액션에서 유도한 둘**로 다시 섰고 **둘 다 0**이다 · 선언 16 vs 러너 5는 **네 provider가 같은 모양**이고 배선된 GCP가 `ValueError`를 **`success: False`**로 돌리니 **결함 아님**. · **②ruff↔pytest 제외 비대칭**: `ruff check`가 같은 트리에서 **20↔6527**을 오갔다(초과분 전부 `cdk.out`) — pytest만 `norecursedirs`로 선언해 뒀다, **Risk 12②가 두 번째 도구에서 재발**; 묻혀 있던 20건 **전수 분류 결함 0**(가장 그럴듯한 `dep_id`는 **08-18의 경계가 쓰는 쪽에서 재확인**된 것). · **①4a의 마지막 칸을 측정으로 닫았다 — **$0.00**이고 사유는 **`Always Free` 40M**: §3이 세운 조건문의 **전건이 거짓**이었다. ⚠️**두 번째 발견 = 파이프가 연속이 아니다** — 13일 중 **4일·41.3h**, 08-27 이후 죽음 → **$1.42는 720h 가정**이라 이 환경에선 원리상 안 난다 · 허용목록 **유출 0**(workspace가 아는 이름이 정확히 4개, 308 시계열이 11일 간격 두 시점에 동일) · 가드 **+8** — 그중 하나가 **이 brief의 미수정 주장을 그 자리에서 red로 잡았다**) · **08-18**(`cost_metrics` 잔여를 측정으로 닫았다 — **결함이 아니라 범위**이고, 면제는 목록이 아니라 **경계**다: 가드가 의무를 읽는 쪽 선택 규칙에서 유도하고 전 모듈을 rglob으로 훑는다, 변이 3종 red · src 변경 0) · **M37**(티어 2가 **조건 평가 없이** 추천에서 액션을 만들어 **에스컬레이션 전용 capability 둘**이 first-response로 실행되고 있었다 — ⚠️**티어 2 액션을 단언하는 테스트가 0개**였다 · 관리형은 매니페스트를 안 내고 **read model이 부재를 설명한다**) · **M36**(ⓒ — 기존 가드가 **생산에서 도달 불가한 capability 집합**으로 물어 **첫-매치와 점수제의 답이 같은 경우만** 태웠다: GCP/Azure는 인시던트와 무관하게 늘 rto **180**을 보고했다, +12) · **M35**(signal↔capability 스윕이 **결함 넷** — `rebalance_consumer`가 **Azure만** 없는데 **같은 커밋에서 태어나 처음부터 빠져 있었다**, +6) · **M26**(렌즈가 `ExecutorOutput`에서 **말랐다**, 동작 변경 0) · **M25**(optional 임포트 **여섯이 미선언** — `.[azure]`엔 LLM조차 없었다, +19) · **M24**(게이트가 **테스트마다 Gemini를 과금 호출** — 288s→39s, D49)) 그 이전(**M23** 이하)은 `COMPLETED_SUMMARY`가 권위.
>
> 1분 압축 문맥. 에이전트 진입점. 이 파일은 **≤60줄**로 유지한다.

## Read Path (순서대로, bulk-read 금지)

1. `docs/AGENT_BRIEF.md` — 이 파일
2. `docs/STATUS.md` — 현재 상태 / 검증 baseline / risks
3. `docs/NEXT_PLAN.md` — 열린 작업만
4. (필요 시) `docs/PROGRESS_LOG.md` 상단 — 최신 증분
5. (필요 시) `docs/engineering/` — harness/loop/context 엔지니어링

권위 순서: `NEXT_PLAN.md` (유일한 source of truth).

## Snapshot

- **무엇:** AWS-native 플랫폼 에이전트. provision → deploy 검증 → detect → analyze → decide → execute → Slack 리포트.
- **동작하는 것:** Operations 4단계 + 3-cloud AI Agent + **On-Prem Ops**(12도구, trace) + Terraform kind/실 Multipass VM Ansible k3s Provision + kagent↔Local Qwen A2A + Agents UI. **On-Prem 오프라인 완결**: Local **Qwen3-Coder-30B-A3B**(MoE·활성 3B, `Makefile:58`)로 NL provision→deploy→validate ~39s, 로컬 JSONL 기록 + 대시보드 **hybrid**(AWS+On-Prem 병합) + 실 **롤백**(app/cluster). **추적 IA**: activity에 `type`(provision/deploy)·`cluster` 연결키, 대시보드 **Provisioning/Deployments/History** 분리 + **중첩 상세**(provisioning⊃deploys), 롤백 **단일-row 승계**·**teardown→deploy cascade**, 자연어 rollback/teardown도 동일 라우팅.
- **하네스:** overnight-harness 플러그인 기반 (5 engine). `make overnight-kiro-once` 로 smoke. `make dev-up`으로 로컬 스택(MLX+proxy+router+dashboard) 한 방 기동.
- **Kiro 특화:** aws-ops / cdk-dev / overnight-harness 3개 에이전트 + safety hook + AWS MCP Server.
- **검증:** `make check` → **2319 passed, 2 skipped** (**2026-08-30 로컬 macOS·py3.13** — 08-18의 **2291**에서 **+28**(4a 청구 **+6** · 증거 포인터 **+2** · ruff↔pytest **+3** · pyproject 주장 **+4** · 로컬 스택 **+5** · provider 계약 일치 **+8**) · CI도 **같은 게이트**를 돈다(ubuntu·py3.13) — **CI 숫자는 PR 체크가 권위**, 여기 복제 금지; 게이트 숫자는 날짜와 **잰 기계** 없이는 주장이 아니다, Risk 12①②). CI(`gate.yml`)가 **main 병합 조건**이고 **terraform 검증까지 실제로 돈다**. 이력·근거는 `STATUS` 검증 Baseline과 `COMPLETED_SUMMARY` **M10~M33**에 있다 — 여기에 다시 적지 말 것.
- **현재 초점:** **Phase 4(billable, 별 승인)만 남았다.** 공급망은 생산자→소비자→CI 키리스까지 섰고 **어드미션만 업스트림 대기**(cosign v3 ↔ policy-controller 저장 위치 불일치, 양방향 실증). Phase 5는 **경계까지**(UI·PR 생성 없음). ⚠️**과대 해석 금지**: 스코프·배포 신원·이미지 서명 게이트는 전부 **옵트인** · 자격증명이 테넌트-바운드인 건 **온프렘뿐** · CODEOWNERS는 **라우팅** · `main` 보호는 **게이트 집행**이지 리뷰 집행이 아니다. **반복 확인된 것**: 새 항목은 **기록된 이유를 한 번 돌려 보고** 시작할 것 — 다만 **틀려 있을 때만 값이 나오는 게 아니다**(08-09 GCP 3건은 전부 성립했는데, 확인하는 과정에서 **프로브의 provider 누락**이 나왔다). 그리고 **읽는 쪽으로 갈 것**: "이 dict를 덮는 테스트가 있나"의 답은 "다섯 개 있다"였는데 **전부 dict의 모양만 물었고**, 읽는 쪽엔 두 provider에 복사된 **죽은 티어**가 있었다(08-13).

## Guardrails

- 에이전트=Python(**3.13**. ⛔`>=3.11`은 08-30에 재서 **거짓이었다** — fresh 3.11은 **2 failed**이고
  같은 의존성의 fresh 3.13은 초록이다. 선언 셋을 3.13으로 맞췄다) / IaC=CDK TS / 모델은 `models.py` 한 곳.
- IAM 최소 권한. ⚠️**`Resource:"*"` 전면 금지는 무조건 거짓이었다** — `incident_agent_stack.ts`의 **7건 전부** AWS가 리소스 타입을 주지 않는 액션이다(08-16 Service Authorization Reference 대조). 규칙은 **"이유를 주석으로 적지 않은 `*` 금지"**이고 이제 **가드가 집행한다**(`test_iam_wildcard_justified.py`). **만족 불가능한 규칙은 우회를 습관으로 만든다.**
- `Delete/Drop/Terminate` 액션은 강제 APPROVE(**D48**로 계약화·집행).
- 요청 이상 기능 추가 금지. 테스트 통과 전 완료 선언 금지.
- Gate 명령: `make check`. **`main`은 보호됨** — 직접 push 불가, PR + CI 통과로만 병합(D43).

## Skills (overnight-harness)

- `/sync` — Read Path 따라 상태 복원(읽기 전용).
- `/checkpoint` — PROGRESS_LOG append + current docs 갱신.
- `/tidy-docs` — 문서 정리/압축.
- `/overnight-report` — 루프 결과 리포트.
- `/overnight-seed` — backlog 시드.
- `/diagnose` — 루프 실패 진단.
