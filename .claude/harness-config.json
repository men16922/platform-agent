{
  "$comment": "Per-repo harness config. Copied to .claude/harness-config.json by /harness-init for historical compatibility across engines. The plugin skills (sync/checkpoint/tidy-docs/overnight-report/overnight-seed) read this to learn your doc filenames, gate command, engine, and line budgets. Edit these to match your repo; skills fall back to these same defaults if the file is absent. Optional: add \"harness_root\": \"/abs/path/to/plugin\" to pin a specific overnight-harness install (else the Makefile auto-resolves the highest installed version).",
  "project_name": "platform-agent",
  "docs": {
    "brief": "docs/AGENT_BRIEF.md",
    "status": "docs/STATUS.md",
    "plan": "docs/NEXT_PLAN.md",
    "log": "docs/PROGRESS_LOG.md",
    "completed": "docs/COMPLETED_SUMMARY.md",
    "decisions": "docs/DECISIONS.md",
    "lessons": "docs/LESSONS.md"
  },
  "archive_dir": "docs/archive",
  "gate": "make check",
  "smoke": "make test",
  "budgets": { "brief": 60, "status": 120, "plan": 120, "log": 120, "lessons": 40 },
  "$char_budgets_comment": "Added 2026-08-30. The line budget above passed all day while AGENT_BRIEF grew to 9,783 chars — one line reached 3,621 (37% of the file). Lines are what /tidy-docs and the budget guard could see; characters are what the agent actually pays. Declared here only (one copy, deliberately): DOCS_POLICY states the line budgets and the docs repeat them, and a fourth spelling of a number is how they drift (M19).",
  "char_budgets": { "brief": 8000, "status": 11000, "plan": 11000, "log": 12000 },
  "engine": "claude",
  "engine_choices": ["claude", "codex", "opencode", "agy", "kiro"]
}
